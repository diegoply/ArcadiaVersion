          
        <div class="col-12 col-md-4 mt-3 d-flex justify-content-center">
        
                <!-- Button trigger modal -->
                <!--Cards-->
            <div class="card bois  habitatsCards" style="width: 18rem;">
                <img src="{{ vich_uploader_asset(habitat.image, 'imageFile') }}" class="card-img-top img-fluid mt-3 habitatsBorder container col-10 imageHabitats" alt="{{habitat.nom}}">
            <div class="card-body">
                <h5 class="card-title Title d-flex justify-content-center mt-2">{{habitat.nom}}</h5>
            <div class="d-flex justify-content-center bois">
                <a href="{{ path('app_AnimauxInHabitat', { 'id': habitat.id } ) }}" class="btn colorbtndark">En savoir plus</a>
            </div>
           
        </div>
        </div>
        </div>
     <!--Finds cards -->
     

  

